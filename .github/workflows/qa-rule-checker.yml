name: QA Rule Checker

# ZERO_BARRIER: quét rule xung đột + auto-fix PR (auto-merge khi CI pass).
# Auto-trigger: schedule 8h + mọi workflow fail trên main.

on:
  schedule:
    - cron: '0 */8 * * *'
  workflow_dispatch:
  workflow_run:
    workflows:
      - "Build and deploy Zola site to GitHub Pages"
      - "QA Gatekeeper"
      - "Auto Merge PRs"
      - "Build Failure Auto-Remediation"
      - "Self-Healing QA"
      - "Compliance Score Audit"
      - "Fetch Build Dashboard"
      - "Fetch Merge Report"
      - "Build Semantic Related Posts"
      - "Performance Audit"
      - "Security Audit"
    types: [completed]

permissions:
  contents: write
  pull-requests: write
  actions: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: qa-rule-checker
  cancel-in-progress: false

jobs:
  rule-check:
    if: >-
      github.repository == 'Banhang-Chogao/zola' &&
      (
        github.event_name != 'workflow_run' ||
        (
          github.event.workflow_run.conclusion == 'failure' &&
          github.event.workflow_run.head_branch == 'main'
        )
      )
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Run QA Rule Checker (scan, report, optional PR)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 scripts/qa-auto-rule-checker.py || true

      - name: Safety guard — never commit main
        run: |
          BR=$(git rev-parse --abbrev-ref HEAD)
          if [ "$BR" = "main" ]; then
            echo "::error::QA Rule Checker không được commit trực tiếp main"
            exit 1
          fi
          echo "Branch: $BR (OK)"