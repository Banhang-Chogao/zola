fastapi==0.137.1
uvicorn[standard]==0.49.0
redis==8.0.0
httpx==0.28.1
feedparser==6.0.12
beautifulsoup4==4.15.0
lxml==6.1.1
# python-multipart bắt buộc cho FastAPI Form() + File() — endpoint
# /cms/author dùng multipart upload avatar. Thiếu → app fail import.
python-multipart==0.0.32
# curl_cffi giả lập TLS fingerprint của Chrome → bypass Cloudflare /
# Akamai detection. Manylinux wheel có sẵn cho Render Python 3.11.
curl-cffi==0.15.0
