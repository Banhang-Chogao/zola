# Dependencies cho scripts/ff.py — AI-Agent debug/deploy Zola.
# Tách khỏi requirements.txt chính để cài tuỳ chọn (chỉ dev local cần).

# --- Core AI client (chọn 1 trong 2) ---
anthropic>=0.40
# openai>=1.50           # uncomment nếu muốn dùng OpenAI thay Anthropic

# --- Logging ---
loguru>=0.7

# --- Code quality (Ruff replaces flake8 + isort + pyupgrade) ---
ruff>=0.6

# --- Static typing ---
mypy>=1.10

# --- Config validation (Pydantic v2) ---
pydantic>=2.7

# --- Git pre-commit hook framework ---
pre-commit>=3.7

# --- Dependency audit (catch ModuleNotFoundError + version conflict sớm) ---
pip-tools>=7.5            # pip-compile + sync khoá version reproducible
pipdeptree>=3.1           # render dependency tree, phát hiện conflict

# --- HTTP retry với exponential backoff ---
tenacity>=9.0             # @retry decorator chống race condition

# --- Test flakiness mitigation ---
pytest-rerunfailures>=16.0  # auto-rerun N lần test fail intermittent
