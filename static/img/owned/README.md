# Ảnh sở hữu (auto-watermark)

Đặt ảnh GỐC do SEOMONEY tự chụp/tạo vào đây để được tự động đóng watermark khi build.
Ảnh bên thứ ba (screenshot app/ngân hàng/logo/promo) KHÔNG để ở đây.
Xem docs/image-watermark.md.
