# Slack Notification Test

File test để kiểm tra workflow `slack-notify.yml` bắn thông báo khi push lên production (`main`).

- Thời điểm test: 16/06/2026 (GMT+7)
- Mục đích: xác nhận `SLACK_WEBHOOK_URL` hoạt động, notification gửi về Slack thành công.
- File này nằm trong `data/`, KHÔNG được Zola publish ra site → an toàn, có thể xoá sau khi test xong.
