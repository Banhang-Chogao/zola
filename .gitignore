public/
.DS_Store
*.swp
__pycache__/
*.pyc
.venv*/
scripts/.venv*/

# Workflow log artifacts — không bao giờ commit
perf-fix.txt
perf-scan.txt
perf-summary.txt
fix-log.txt
fixed-summary.txt
pr-body.md
reports/performance/*.json
!reports/performance/.gitkeep

# Private agent memory / local strategy (see CULTURE_OF_DEPLOYMENT.md §6)
# Local-only tactical notes for Claude/OpenCode — NEVER commit. CI must not depend on these.
CLAUDE_PRIVATE.md
claude-private.md
.claude-private.md
.claude/private/

# Sensitive: never commit tokens/keys
ghp_*
*.pem
*.key
.env
.env.*

# Paywall runtime data — không commit DB local / backup strip tạm
data/paywall.db
data/.paywall-content-backup.json

# VIPZone backend local/test SQLite (default path khi không set VIPZONE_DB_PATH)
data/vipzone.db

# Changelog data — moved to VIPZone backend API (admin-gated)
# Never commit changelog.json locally; all entries pushed to backend
changelog.json

# ff.py AI-Agent — logs + teach feedback (local only)
.ff/

# Generated pagination anchors (scripts/build_feed_pagination.py)
content/feed-anchor-*.md
content/posting/feed-anchor-*.md
node_modules/
