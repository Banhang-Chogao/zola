public/
.DS_Store
*.swp
__pycache__/
*.pyc

# Workflow log artifacts — không bao giờ commit
perf-fix.txt
perf-scan.txt
perf-summary.txt
fix-log.txt
fixed-summary.txt
pr-body.md

# Sensitive: never commit tokens/keys
ghp_*
*.pem
*.key
.env
.env.*

# ff.py AI-Agent — logs + teach feedback (local only)
.ff/

# Zola binary tooling extracted vào repo lúc setup môi trường (man pages +
# license của Zola) — KHÔNG phải nội dung blog, không commit.
artifacts/
LICENSE
